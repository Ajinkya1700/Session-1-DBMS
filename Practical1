Drop table if exists student;
create table student(
  student_id int,
  name varchar(20),
  marks int
);

insert into student(student_id,name,marks) value(1,'Rahul','60');
insert into student(student_id,name,marks) value(2,'Manthan','70');

select*from student where marks='60';
select*from student where marks='70';
